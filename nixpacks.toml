[phases.setup]
nixPkgs = ["...", "conda"]
cmds = ["chmod +x start.sh", "./start.sh"]
[phases.install]
cmds = ["conda install pip && conda create --name venv && conda activate venv && conda install --file requirements.txt"]
